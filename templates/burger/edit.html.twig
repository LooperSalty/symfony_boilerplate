{% extends 'base.html.twig' %}

{% block title %}Modifier le Burger{% endblock %}

{% block body %}
    <h1>Modifier le Burger</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning">Modifier</button>
    {{ form_end(form) }}

    <a href="{{ path('burger_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
